{
  "title": "AdGuard Policy Sync",
  "config": {
    "step": {
      "user": {
        "title": "Connect to AdGuard Home",
        "description": "Enter the AdGuard Home URL (direct port, not Ingress) and (if set) credentials. Optionally provide a JSON file name in /config to sync devices and rules.",
        "data": {
          "base_url": "Base URL (e.g. http://10.2.0.10:3000)",
          "username": "Username",
          "password": "Password",
          "verify_ssl": "Verify TLS certificates",
          "skip_test": "Skip connectivity test (advanced)",
          "devices_json": "Devices JSON file (relative to /config)",
          "rules_text": "Custom rules to apply (leave empty to use defaults)"
        }
      }
    },
    "error": {
      "cannot_connect": "Cannot reach AdGuard Home with those settings (see Logs for details)",
      "password_required": "Password is required when a username is provided"
    }
  }
}
